// Split

// The Split() method splits strings returning an array, without changing the original.

// (' ') Is used as a separator, the string is split between strings.

// Example

let string = 'This is a string';
let splitString = string.split(' ');
console.log(splitString);

// Returns
// [object Array] (4)
// ["This","is","a","string"]